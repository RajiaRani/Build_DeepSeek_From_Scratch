# Python virtual environments (rebuildable — never commit)
.venv/
venv/
env/

# Python caches
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/

# Jupyter
.ipynb_checkpoints/
*/.ipynb_checkpoints/*

# OS / editor junk
.DS_Store
Thumbs.db
.vscode/
.idea/

# Model weights / large data (add explicitly if you really want them tracked)
*.pt
*.pth
*.ckpt
*.safetensors
*.bin

# Downloaded dataset (recreate with the curl cell)
input.txt
